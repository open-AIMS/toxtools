% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_workbook.R
\name{run_workbook}
\alias{run_workbook}
\title{Analyse every sheet in a workbook and write a report}
\usage{
run_workbook(
  path = find_workbook(),
  output_dir = "outputs",
  sheets = NULL,
  model = "decline",
  predictor_transform = "sqrt",
  ecx_vals = c(10, 50),
  refit = FALSE,
  render = TRUE,
  format = "pdf",
  ...
)
}
\arguments{
\item{path}{Path to the workbook. The default takes the single workbook in
\verb{inputs/}.}

\item{output_dir}{Folder to write results into. A sub-folder named after the
workbook is created inside it.}

\item{sheets}{Optional character vector naming the sheets to analyse. The
default analyses every sheet identified as holding fittable data.}

\item{model, predictor_transform, ecx_vals}{Passed to \code{\link[=fit_analysis_sheet]{fit_analysis_sheet()}}
and \code{\link[=workbook_estimates]{workbook_estimates()}}.}

\item{refit}{If \code{TRUE}, refit sheets that already have a saved fit.}

\item{render}{If \code{FALSE}, fit and save but do not render the report.}

\item{format}{Report format, passed to Quarto. \code{"pdf"} by default;
\code{"html"} needs no LaTeX installation.}

\item{...}{Further arguments passed to \code{\link[=fit_analysis_sheet]{fit_analysis_sheet()}}, and from
there to \code{\link[bayesnec:bnec]{bayesnec::bnec()}} and \code{\link[brms:brm]{brms::brm()}}; for example
\code{backend = "cmdstanr"}, \code{iter} or \code{cores}.}
}
\value{
Invisibly, the path to the manifest file describing the run.
}
\description{
The single call that runs the whole workflow. It finds the workbook, decides
which of its sheets hold concentration-response data, fits the \code{bayesnec}
model set to each of them, and renders one PDF holding the results for every
sheet.
}
\details{
Fitting is the slow step, taking roughly twenty to forty minutes per sheet
on four cores. Each fit is saved as it completes, and a fit that is already
saved is reused rather than repeated, so an interrupted run can be restarted
by calling the function again. A saved fit is reused only when it was fitted
with the same model set, the same predictor transformation and the same
sheet; changing any of them causes it to be refitted.

A sheet that cannot be analysed does not stop the run. The reason is
reported, recorded in \code{sheet_classification.csv} and carried in the
manifest, and the remaining sheets are still fitted and reported; a run
stops only when no sheet at all could be analysed. A run covering several
sheets takes hours, and discarding the ones that worked because a later one
failed would waste them.
}
\examples{
\dontrun{
run_workbook()
}
}
