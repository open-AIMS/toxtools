% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_workbook.R
\name{plot_workbook_fit}
\alias{plot_workbook_fit}
\title{Plot a fitted model set}
\usage{
plot_workbook_fit(x, estimates = NULL, all_models = FALSE, title = NULL)
}
\arguments{
\item{x}{An \code{analysis_fit} object from \code{\link[=fit_analysis_sheet]{fit_analysis_sheet()}}.}

\item{estimates}{Optional data frame from \code{\link[=workbook_estimates]{workbook_estimates()}}. When
supplied and \code{all_models} is \code{FALSE}, the no-effect concentration and its
credible interval are drawn on the plot and labelled in the recorded
units.}

\item{all_models}{If \code{TRUE}, one panel per retained equation instead of the
model-averaged curve.}

\item{title}{Plot title. \code{NULL} uses the sheet name.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the model-averaged fit, or the individual equations that make it up,
with the concentration axis spaced on the scale the curve was fitted on and
labelled in the units recorded on the sheet.
}
\details{
The axis is spaced by the square root (or logarithm) of concentration,
which is the scale the curve was fitted on and which spreads out the low
concentrations where the response turns. Only the spacing is transformed:
the ticks are labelled with the concentrations themselves, so the axis can
be read in the units on the sheet.

\code{bayesnec} returns plot data on the recorded concentration scale even when
the curve was fitted to a transformed predictor, so the spacing is applied
by the scale rather than by transforming the data. Its \code{nec()} and \code{ecx()}
estimates, by contrast, come back on the fitted scale; \code{\link[=workbook_estimates]{workbook_estimates()}}
back-transforms those, which is why the value annotated here needs no
further conversion.
}
\examples{
\dontrun{
plot_workbook_fit(fit, workbook_estimates(fit))
}
}
