% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_workbook.R
\name{fit_analysis_sheet}
\alias{fit_analysis_sheet}
\title{Fit the bayesnec model set to one analysis sheet}
\usage{
fit_analysis_sheet(
  sheet,
  model = "decline",
  predictor_transform = c("sqrt", "log", "identity"),
  chains = 4,
  cores = chains,
  seed = 101,
  ...
)
}
\arguments{
\item{sheet}{An \code{analysis_sheet} object from \code{\link[=read_analysis_sheet]{read_analysis_sheet()}}.}

\item{model}{Model set passed to \code{\link[bayesnec:bnec]{bayesnec::bnec()}}. The default,
\code{"decline"}, fits the equations that decrease monotonically with
concentration. Use \code{"all"} to include the hormesis equations, which allow
the response to rise before it falls.}

\item{predictor_transform}{One of \code{"sqrt"}, \code{"log"} or \code{"identity"}.}

\item{chains, cores, seed}{Passed to \code{\link[bayesnec:bnec]{bayesnec::bnec()}}.}

\item{...}{Further arguments passed to \code{\link[bayesnec:bnec]{bayesnec::bnec()}}, and from there to
\code{\link[brms:brm]{brms::brm()}}; for example \code{iter}, \code{backend} or \code{control}.}
}
\value{
An object of class \code{analysis_fit}: a list holding the fit
(\code{fit}), the \code{analysis_sheet} it was fitted to, the formula used and the
transformation functions.
}
\description{
Fits a set of concentration-response curves to the data read from one sheet
and returns the model-averaged fit, together with the information the report
needs to describe it.
}
\details{
Concentration is transformed before fitting, by default with a square root.
The square root is the default rather than a logarithm because these designs
include a zero control: a logarithm of zero is undefined, so a log-scaled
fit would either drop the controls or require an arbitrary offset added to
every concentration. The square root spreads the low concentrations, where
the curve turns, without either.

The response is fitted as recorded, not as a per cent of control.
\code{\link[bayesnec:ecx]{bayesnec::ecx()}} measures decline relative to the fitted control value one
posterior draw at a time, so it already accounts for uncertainty in the
control; dividing by the observed control mean beforehand discards that
uncertainty and biases the effect concentrations upwards.
}
\examples{
\dontrun{
s <- read_analysis_sheet(find_workbook(), "Copper Ref (4)")
fit_analysis_sheet(s)
}
}
