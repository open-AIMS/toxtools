% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_app.R
\name{run_app}
\alias{run_app}
\title{Run the analysis through a point-and-click interface}
\usage{
run_app(
  input_dir = "inputs",
  output_dir = "outputs",
  launch_browser = TRUE,
  ...
)
}
\arguments{
\item{input_dir}{Folder holding the workbooks to choose between. Workbooks
added through the page are copied into it.}

\item{output_dir}{Folder to write results into.}

\item{launch_browser}{Whether to open a browser window.}

\item{...}{Further arguments passed to \code{\link[shiny:runApp]{shiny::runApp()}}.}
}
\value{
Called for its side effect. Returns nothing.
}
\description{
Opens a small web page in the browser that drives the same workflow as
\code{\link[=run_workbook]{run_workbook()}}: choose a workbook, see which of its sheets hold data that
can be fitted, choose the settings, start the analysis, watch it run, and
download the report. Nothing has to be typed.
}
\details{
The page runs on the machine it is started from. The workbook is not sent
anywhere, and the results are written to the same \verb{outputs/} folder that
\code{\link[=run_workbook]{run_workbook()}} writes to.

Fitting is done in a separate R process, so the page stays responsive for
the twenty to forty minutes each sheet takes, and closing the browser tab by
accident does not stop the analysis. The process is stopped when the app
itself is stopped.
}
\examples{
\dontrun{
run_app()
}
}
